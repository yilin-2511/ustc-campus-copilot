# 竞品调研与技术分析

> **队伍**：CloseClaw  
> **分析日期**：2026年6月25日  
> **分析目的**：为"一〇七杯"智能体开发大赛提供竞品参考和技术方案

---

## 目录

1. [竞品项目分析](#1-竞品项目分析)
2. [技术框架调研](#2-技术框架调研)
3. [多源信息整合方案](#3-多源信息整合方案)
4. [对 USTC 的启示](#4-对-ustc-的启示)
5. [行动建议](#5-行动建议)

---

## 1. 竞品项目分析

### 1.1 SJTU Agent（上海交通大学）

#### 基本信息
- **项目地址**：https://github.com/kuan-er/sjtu-agent
- **启动时间**：2026年5月5日
- **当前版本**：v0.6.3（12次发布）
- **开发周期**：45天
- **Stars**：69+
- **技术栈**：Python + Playwright + Flask + 自研 Agent 框架

#### 核心功能

| 类别 | 功能 | 实现方式 | 数据源 |
|------|------|----------|--------|
| **学业管理** | 课表查询 | 爬虫 + API | 教学信息服务网 |
| | 成绩查询 | 爬虫 + API | 教学信息服务网 |
| | DDL 聚合 | 多平台集成 | Canvas, MOOC, AI好课 |
| | 作业助手 | Canvas + Claude Code | Canvas LMS |
| **校园生活** | 食堂推荐 | 实时拥挤度 + 历史偏好 | campuslife API |
| | 食堂拥挤度 | 实时数据 | campuslife API |
| | 校园门户 | 浏览器自动化 | my.sjtu.edu.cn |
| | 班车预约 | 浏览器自动化 | my.sjtu.edu.cn |
| **信息服务** | 每日报告 | 定时推送 | 多源聚合 |
| | 校园新闻 | 爬虫聚合 | 多源 |
| | 邮件监控 | API 集成 | 校园邮件 |
| **平台接入** | Telegram/飞书/微信/QQ | Bot API | 多平台 |

#### 技术架构

```
sjtu-agent/
├── agent.py                  # 兼容入口
├── sjtu_agent/
│   ├── agent/                # 核心 Agent 逻辑
│   │   ├── prompts.py        # 系统提示词
│   │   ├── tools/            # 工具集
│   │   └── chat_loop.py      # 对话循环
│   ├── data/                 # 数据模块
│   ├── web/                  # Web UI
│   ├── news_aggregator/      # 新闻聚合
│   └── ...
├── telegram_bot.py           # Telegram Bot
├── feishu_bot.py             # 飞书 Bot
├── wechat_bot.py             # 微信 Bot
└── mcp_server.py             # MCP Server
```

#### 设计亮点

1. **工具调用设计**：清晰的工具路由策略
2. **智能提示词**："永远先尝试，不主动说不行"
3. **浏览器自动化**：Playwright 自动化校园门户
4. **多平台接入**：Telegram/飞书/微信/QQ
5. **新闻聚合系统**：完整的采集→去重→排序→推送流程

#### 优点与缺点

| 优点 | 缺点 |
|------|------|
| ✅ 功能全面 | ❌ 自研框架维护成本高 |
| ✅ 真实数据 | ❌ 爬虫需要维护 |
| ✅ 多平台支持 | ❌ 单 Agent 架构 |
| ✅ 成熟稳定 | ❌ 个性化较基础 |

---

### 1.2 campus-info-agent

#### 基本信息
- **项目地址**：https://github.com/Re7-re/campus-info-agent
- **技术栈**：Python + LangGraph + LangChain + 智谱AI GLM-4 + Gradio
- **数据源**：模拟数据（非真实）

#### 核心功能

| 功能 | 说明 |
|------|------|
| 成绩查询 | 模拟数据 |
| 课表查询 | 模拟数据 |
| 空教室查询 | 模拟数据 |
| 考试安排 | 模拟数据 |
| 校园通知 | 模拟数据 |
| 知识库问答 | 内置知识库 |

#### 技术架构

```
campus-info-agent/
├── agent/                  # 智能体核心模块
├── features/              # 功能模块目录
│   ├── base_feature.py    # 功能模块基类
│   ├── agent_feature.py   # AI智能助手功能
│   ├── grade_feature.py   # 成绩查询功能
│   ├── schedule_feature.py # 课表查询功能
│   ├── classroom_feature.py # 教室查询功能
│   ├── exam_feature.py    # 考试查询功能
│   └── notice_feature.py  # 通知查询功能
├── utils/                 # 工具模块目录
├── ui/                    # 用户界面模块
└── data/                  # 数据模块
```

#### 设计亮点

1. **模块化设计**：每个功能独立模块
2. **LangGraph 状态机**：清晰的工作流
3. **Gradio UI**：美观的 Web 界面
4. **知识库支持**：内置校园知识库

#### 优点与缺点

| 优点 | 缺点 |
|------|------|
| ✅ 使用成熟框架 | ❌ 模拟数据（非真实） |
| ✅ 模块化设计 | ❌ 单 Agent 架构 |
| ✅ 易于扩展 | ❌ 功能相对简单 |
| ✅ UI 美观 | ❌ 无个性化 |

---

### 1.3 竞品对比总结

| 维度 | SJTU Agent | campus-info-agent | 我们的机会 |
|------|-----------|-------------------|-----------|
| **数据源** | 真实数据（爬虫+API） | 模拟数据 | **真实数据** |
| **架构** | 单 Agent | 单 Agent + 模块化 | **多智能体协作** |
| **框架** | 自研 | LangGraph | **LangChain + CrewAI** |
| **个性化** | 基础 | 无 | **深度个性化** |
| **情境感知** | 无 | 无 | **时间/地点/天气** |
| **目标用户** | 全体在校生 | 学生 | **大一新生** |
| **平台** | 多平台 Bot | Web UI | **Web + 微信/钉钉** |

---

## 2. 技术框架调研

### 2.1 主流 Agent 框架对比

| 特性 | LangChain | CrewAI | AutoGen |
|------|-----------|--------|---------|
| **Stars** | 90k+ | 20k+ | 40k+ |
| **学习曲线** | 中等 | 简单 | 中等 |
| **多智能体** | 支持 | **原生支持** | **原生支持** |
| **工具生态** | **丰富** | 中等 | 中等 |
| **RAG 支持** | **优秀** | 良好 | 良好 |
| **企业采用** | **广泛** | 增长中 | 微软内部 |
| **适合场景** | 通用 | 团队协作 | 复杂对话 |

### 2.2 推荐技术栈

| 组件 | 推荐方案 | 理由 |
|------|---------|------|
| **Agent 框架** | LangChain + CrewAI | 成熟 + 多智能体 |
| **Web 框架** | FastAPI | 现代 + 异步 |
| **前端** | Streamlit/Gradio | 快速开发 |
| **浏览器自动化** | Playwright | 成熟稳定 |
| **向量数据库** | ChromaDB | 轻量级 |
| **Bot 平台** | 微信/钉钉 | 国内主流 |
| **模型** | DeepSeek-V4/GLM5.2 | 比赛提供 |

### 2.3 多智能体协作架构

```
┌─────────────────────────────────────┐
│           用户请求                    │
│  "帮我规划下周学习安排"               │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│         Router Agent                │
│  - 意图识别                          │
│  - 任务分解                          │
│  - 智能体分配                        │
└──────────────┬──────────────────────┘
               │
      ┌────────┼────────┐
  ┌────▼────┐ ┌▼────────┐ ┌▼──────────┐
  │学习助手  │ │生活助手  │ │社交助手    │
  │          │ │         │ │           │
  │- 课表    │ │- 食堂   │ │- 社团     │
  │- 图书馆  │ │- 交通   │ │- 活动     │
  │- 作业    │ │- 住宿   │ │- 交友     │
  └────┬────┘ └───┬─────┘ └───┬───────┘
       └──────────┼───────────┘
                  │
┌─────────────────▼──────────────────┐
│       Synthesizer Agent            │
│  - 结果整合                         │
│  - 个性化调整                       │
│  - 生成最终回答                     │
└────────────────────────────────────┘
```

---

## 3. 多源信息整合方案

### 3.1 核心架构

```
定时任务触发 → 并发采集 → 去重 → 时间过滤 → 用户画像 → 智能排序 → 日报生成 → 多平台推送 → 存储
```

### 3.2 统一数据结构

```python
@dataclass
class NewsItem:
    """统一的新闻数据结构。"""
    id: str                          # 全局唯一 ID（source:url_hash）
    source: str                      # 来源标识（jwc / shuiyuan / official / canvas）
    title: str                       # 标题
    summary: str                     # 摘要（200 字以内）
    url: str                         # 原文链接
    published_at: datetime           # 发布时间（必须带 CST 时区）
    author: str = ""                 # 作者/部门
    category: str = ""               # 分类
    tags: list[str] = field(default_factory=list)
```

### 3.3 信息源抽象基类

```python
class BaseNewsSource(ABC):
    """所有信息源的抽象基类。"""
    name: str = "base"
    enabled: bool = True

    def fetch_recent(self, hours: int = 24) -> list[NewsItem]:
        """获取最近 N 小时的新内容。失败时返回空列表，不抛异常。"""
        if not self.enabled:
            return []
        try:
            return self._fetch(hours)
        except Exception as e:
            print(f"[news/{self.name}] 采集失败：{e}", flush=True)
            return []

    @abstractmethod
    def _fetch(self, hours: int) -> list[NewsItem]:
        """子类实现具体采集逻辑。"""
        ...
```

### 3.4 各信息源实现

| 信息源 | 获取方式 | 参考实现 |
|--------|----------|----------|
| **教务系统** | 爬虫/API | JwcSource |
| **图书馆** | API | CanvasSource |
| **食堂** | 爬虫 | JwcSource |
| **活动平台** | 爬虫 | ShuiyuanSource |
| **QQ 群** | 需要探索 | 暂无参考 |
| **微信公众号** | RSS/爬虫 | WechatMpSource |

### 3.5 智能排序

```python
class NewsRanker:
    def rank(self, items: list[NewsItem], profile: UserProfile, top_k: int = 8) -> list:
        # 1. 关键词初筛（快速过滤）
        scored = []
        for item in items:
            score = self._keyword_score(item, profile)
            scored.append((item, score))
        
        # 2. 取 top 30 送 LLM 精排
        scored.sort(key=lambda x: x[1], reverse=True)
        top_30 = scored[:30]
        
        # 3. LLM 精排
        llm_scored = self._llm_rank(top_30, profile)
        
        # 4. 返回 top_k
        return llm_scored[:top_k]
```

### 3.6 多平台推送

```python
class Notifier(ABC):
    @abstractmethod
    def send(self, title: str, body: str, priority: str = "normal") -> bool:
        """发送通知，返回是否成功"""
        ...

class NotificationDispatcher:
    def __init__(self, config: dict):
        self.notifiers: list[Notifier] = []
        # 根据 config 初始化 TelegramNotifier / WeChatNotifier / SystemNotifier
    
    def send(self, title: str, body: str, priority: str = "normal"):
        """向所有已配置渠道发送"""
        for n in self.notifiers:
            try:
                n.send(title, body, priority)
            except Exception as e:
                logging.error(f"Notifier {n} failed: {e}")
```

### 3.7 去重与存储

```python
class NewsStorage:
    """管理已推送新闻 ID，防止重复推送。"""
    
    def is_pushed(self, news_id: str) -> bool:
        data = self._load()
        return news_id in data.get("pushed", {})

    def mark_pushed(self, news_ids: list[str]) -> None:
        """标记这批新闻已推送。"""
        data = self._load()
        pushed = data.get("pushed", {})
        now_ts = time.time()
        for nid in news_ids:
            pushed[nid] = now_ts
        # 清理 7 天前的记录
        cutoff = now_ts - 7 * 86400
        pushed = {k: v for k, v in pushed.items() if v > cutoff}
        data["pushed"] = pushed
        self._save(data)
```

---

## 4. 对 USTC 的启示

### 4.1 数据源获取策略

| 数据源 | SJTU 方式 | USTC 可能方式 |
|--------|----------|--------------|
| **食堂数据** | campuslife API | 需要探索公开 API 或爬虫 |
| **课表成绩** | 爬虫 + 模拟登录 | 类似方式 |
| **作业平台** | Canvas API | 需要确认 USTC 平台 |
| **校园门户** | my.sjtu.edu.cn | USTC 校园门户 |
| **图书馆** | 浏览器自动化 | 类似方式 |

### 4.2 科大现有资源平台

| 类别 | 平台 | 功能 | 访问方式 |
|------|------|------|----------|
| **教务系统** | 综合教务系统 | 课表、成绩、选课 | https://jw.ustc.edu.cn/ |
| | 瀚海教学网 | 在线课程、教学资料 | https://course.ustc.edu.cn/ |
| | Blackboard | 国际课程平台 | https://www.bb.ustc.edu.cn/ |
| | 蜗壳学社 | 学生社区、学习交流 | https://ot.ustc.edu.cn/ |
| **图书馆** | 图书馆主页 | 馆藏查询、借阅 | https://www.lib.ustc.edu.cn/ |
| | 学习空间预约 | 自习室、研讨室预约 | 图书馆网站 |
| **校园服务** | 学工在线 | 学生事务、请假 | https://stuhome.ustc.edu.cn/ |
| | 一号通办 | 一站式服务 | 学校官网 |
| | 教室报修 | 教学设备报修 | https://fix.ustc.edu.cn/ |

### 4.3 非正式信息渠道

| 渠道类型 | 示例 | 信息内容 | 特点 |
|----------|------|----------|------|
| **QQ 群** | 新生广场、大物实验群、创新实践基地群、【次元计划】交流群 | 课程资料、实验指导、项目信息、经验分享 | 信息密度低、需要翻找 |
| **微信群** | 班级群、社团群、实验室群 | 通知、活动、资料分享 | 信息碎片化、易错过 |
| **论坛** | 瀚海星云、知乎、小红书 | 经验帖、攻略、评价 | 信息质量参差不齐 |
| **口口相传** | 学长学姐、同学 | 实用建议、避坑指南 | 真实但效率低 |

### 4.4 我们的差异化优势

| 维度 | 现有项目 | 我们的优势 |
|------|---------|-----------|
| **架构** | 单 Agent | **多智能体协作** |
| **框架** | 自研/LangGraph | **LangChain + CrewAI** |
| **个性化** | 基础/无 | **深度个性化** |
| **情境感知** | 无 | **时间/地点/天气** |
| **目标用户** | 全体 | **聚焦新生** |
| **数据源** | 爬虫/模拟 | **真实数据 + API** |
| **平台** | 多平台/Web | **Web + 微信/钉钉** |

### 4.5 功能优先级

#### P0 - MVP（2-3周）
- [ ] 智能问答（RAG）
- [ ] 食堂推荐
- [ ] 校园导览与信息查询

#### P1 - 重要功能（3-4周）
- [ ] 课表查询
- [ ] 活动推荐
- [ ] 自习室查询

#### P2 - 扩展功能（比赛前）
- [ ] 成绩查询
- [ ] DDL 聚合
- [ ] 个性化推荐

---

## 5. 行动建议

### 5.1 学习借鉴
- [ ] 研究 SJTU Agent 的工具调用设计
- [ ] 学习 campus-info-agent 的模块化设计
- [ ] 参考两者的提示词工程
- [ ] 研究新闻聚合系统的实现

### 5.2 差异化创新
- [ ] 多智能体协作架构
- [ ] 深度个性化推荐
- [ ] 情境感知服务
- [ ] 聚焦新生场景

### 5.3 技术实现
- [ ] 使用 LangChain + CrewAI
- [ ] 使用 FastAPI + Streamlit
- [ ] 使用 Playwright（浏览器自动化）
- [ ] 使用 ChromaDB（向量数据库）
- [ ] 实现多源信息整合系统

### 5.4 注意事项

| 风险 | 对策 |
|------|------|
| **数据获取合法性** | 遵守学校的数据使用政策，优先使用公开 API |
| **爬虫维护成本** | 建立数据更新机制，监控页面结构变化 |
| **LLM API 配额** | 关键词初筛后只送 top 30 进 LLM |
| **信息源访问失败** | 单源失败不影响整体；连续 3 天失败自动禁用 |
| **用户画像错误更新** | 衰减机制：weights *= 0.95/天 |
| **推送疲劳** | score_threshold 默认 0.5；用户可调 |
| **隐私保护** | 默认本地不上传；提供清除命令 |
| **重复推送** | NewsStorage 维护 7 天 ID 集合，跨日去重 |

---

## 附录

### A. 参考资料

#### 技术文档
- LangChain 官方文档：https://python.langchain.com/
- CrewAI 文档：https://docs.crewai.com/
- LlamaIndex 文档：https://docs.llamaindex.ai/

#### 开源项目
- LangChain：https://github.com/langchain-ai/langchain
- CrewAI：https://github.com/crewAIInc/crewAI
- AutoGen：https://github.com/microsoft/autogen
- SJTU Agent：https://github.com/kuan-er/sjtu-agent
- campus-info-agent：https://github.com/Re7-re/campus-info-agent

#### 相关论文
- arXiv:2602.14831 - Robot-Wearable Conversation Hand-off for Navigation
- arXiv:2510.26999 - AIOT based Smart Education System

### B. 国内外高校 AI 助手案例

| 学校 | 产品名称 | 特点 | 技术栈 |
|------|---------|------|--------|
| **MIT** | MIT Assistant | 课程推荐、学术资源导航 | LLM + RAG |
| **斯坦福** | Stanford AI Tutor | 个性化学习辅导 | Multi-Agent + RAG |
| **清华** | 清华智能助手 | 校园服务、信息查询 | LLM + 知识图谱 |
| **北大** | 北大智慧校园 | 综合服务平台 | 微服务 + AI |
| **浙大** | 浙大 AI 助手 | 教学、科研、生活全覆盖 | Multi-Agent |

### C. 商业产品参考

| 产品 | 类型 | 可借鉴点 |
|------|------|----------|
| **Notion AI** | 文档助手 | 自然语言交互、上下文理解 |
| **Microsoft Copilot** | 办公助手 | 多工具集成、主动建议 |
| **飞书智能伙伴** | 协作助手 | 情境感知、个性化推荐 |
| **钉钉 AI 助理** | 企业助手 | 多智能体协作、工作流自动化 |

---

*最后更新：2026年6月25日*  
*整合自：SJTU_Agent_项目分析.md、校园Copilot调研报告.md、项目对比分析.md、多源信息整合与推送技术方案.md*